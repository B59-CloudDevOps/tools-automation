input {
    beats {
      port => 5044
    }
  
   }
  output {
      elasticsearch {
          hosts => ["https://localhost:9200"]
          data_stream => "auto"
          ssl_verification_mode => "none"
          user => "{{ secrets.elk_user }}"
          password => "{{ secrets.elk_password }}"
      }
  }